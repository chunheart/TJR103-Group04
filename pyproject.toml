[tool.poetry]
name = "icook-crawler"
version = "0.1.0"
description = "iCook 食譜自動爬蟲（每日排程版）"
authors = ["Your Name <you@example.com>"]
readme = "README_PowerShell_Docker.md"

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31.0"
beautifulsoup4 = "^4.12.3"
pandas = "^2.2.2"
pytz = "^2024.1"
lxml = "^5.2.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
